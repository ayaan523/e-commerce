# Root .env example - copy and update this for your local development
# Frontend env vars are in frontend/.env (copy from frontend/.env.example)
# Backend env vars are in backend/.env (copy from backend/.env.example)

# For Docker Compose, these should be set in backend/.env and frontend/.env.local
